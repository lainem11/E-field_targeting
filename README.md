# E-field targeting

Open compute_targets.m which contains an example of E-field targeting. You need to specify paths to E-field and headmodel files with specific format (TODO).

Required MATLAB add-ons (TODO):
optimization toolbox
